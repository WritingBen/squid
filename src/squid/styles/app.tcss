/* Squid - CMUS-style YouTube Music TUI */
/* Zenburn-inspired theme matching foot terminal */
/* bg: #1c1c1c  fg: #dcdccc  accent: muted warm tones */

/* Base styles - transparent to show terminal background */
Screen {
    background: ansi_default;
}


/* Containers transparent */
Vertical {
    background: ansi_default;
}

Horizontal {
    background: ansi_default;
}

Container {
    background: ansi_default;
}

/* View container */
.view-container {
    width: 100%;
    height: 1fr;
}

/* Content row - main content + volume slider */
#content-row {
    width: 100%;
    height: 1fr;
}

/* Main content area - where views are mounted */
#main-content {
    width: 1fr;
    height: 100%;
}

#main-content > * {
    width: 100%;
    height: 100%;
}

/* Volume column on the right */
#volume-column {
    width: 8;
    height: 100%;
}

/* Spacer to push volume slider to bottom */
#volume-spacer {
    height: 1fr;
}

/* Volume slider - 1/4 of window height */
#volume-slider {
    width: 100%;
    height: 8;
}

/* Headers - muted dark surface */
.header {
    height: 1;
    background: #3f3f3f;
    color: #dcdccc;
    padding: 0 1;
    text-style: bold;
}

/* Pane headers */
.pane-header {
    height: 1;
    background: #3f3f3f;
    color: #dcdccc;
    padding: 0 1;
}

/* Status elements */
.status-line {
    height: 1;
    background: #2e2e2e;
    color: #6f6f6f;
}

/* Data tables */
DataTable {
    scrollbar-size: 1 1;
    background: ansi_default;
}

/* Custom widgets */
TrackList {
    background: ansi_default;
}

ArtistTree {
    background: ansi_default;
}

DataTable > .datatable--cursor {
    background: #4f4f4f;
    color: #f0dfaf;
    text-style: bold;
}

DataTable > .datatable--header {
    background: #3f3f3f;
    color: #dcdccc;
    text-style: bold;
}

/* Trees */
Tree {
    scrollbar-size: 1 1;
    background: ansi_default;
}

Tree > .tree--cursor {
    background: #4f4f4f;
    color: #f0dfaf;
    text-style: bold;
}

Tree > .tree--label {
    color: #dcdccc;
}

Tree > .tree--guides {
    color: #4f4f4f;
}

/* Inputs */
Input {
    border: none;
    background: #2e2e2e;
    padding: 0 1;
}

Input:focus {
    border: none;
    background: #3f3f3f;
}

/* Buttons */
Button {
    min-width: 16;
    background: #3f3f3f;
    color: #dcdccc;
}

Button:hover {
    background: #4f4f4f;
}

/* Progress bars */
ProgressBar {
    padding: 0;
}

ProgressBar Bar {
    width: 1fr;
}

ProgressBar Bar > .bar--bar {
    color: #8cd0d3;
}

ProgressBar Bar > .bar--complete {
    color: #7f9f7f;
}

/* Radio buttons */
RadioSet {
    background: ansi_default;
    border: none;
    height: auto;
    padding: 0;
}

RadioButton {
    background: ansi_default;
    border: none;
    padding: 0 1;
}

RadioButton:focus {
    text-style: bold;
}

/* Help text */
.help-text {
    color: #6f6f6f;
    text-style: italic;
}

/* Error text */
.error-text {
    color: #cc9393;
}

/* Success text */
.success-text {
    color: #7f9f7f;
}

/* Loading indicator */
.loading {
    text-align: center;
    color: #6f6f6f;
}

/* Header and Menubar */
Header {
    background: #3f3f3f;
    color: #dcdccc;
}

/* Menubar (uses Footer widget, docked to top) */
Footer {
    width: 100%;
    dock: top;
    background: #2e2e2e;
    color: #dcdccc;
}

FooterKey {
    width: auto;
    background: #2e2e2e;
    color: #dcdccc;
    .footer-key--key {
        background: #2e2e2e;
        color: #f0dfaf;
        text-style: none;
    }
    .footer-key--description {
        background: #2e2e2e;
        color: #dcdccc;
        text-style: none;
    }
}

FooterKey:hover {
    background: #4f4f4f;
    color: #dcdccc;
    text-style: none;
    .footer-key--key {
        background: #4f4f4f;
        color: #f0dfaf;
        text-style: none;
    }
    .footer-key--description {
        background: #4f4f4f;
        color: #dcdccc;
        text-style: none;
    }
}

/* Status bar */
StatusBar {
    width: 100%;
    height: 1;
    background: #2e2e2e;
}

/* PlayBar - transport controls and progress */
PlayBar {
    width: 100%;
    height: 3;
    background: #2e2e2e;
}

PlayBar .transport-btn {
    min-width: 4;
    width: 4;
    height: 1;
    border: none;
    background: transparent;
    padding: 0;
}

PlayBar .transport-btn:hover {
    background: #4f4f4f;
    color: #dcdccc;
}

PlayBar .transport-btn:focus {
    background: #3f3f3f;
}
